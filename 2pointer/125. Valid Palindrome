class Solution(object):
    def isPalindrome(self, s):
        lst=[]
        for char in s:
            if char.isalnum()==True:
                lst.append(char.lower())
        return lst==lst[::-1]
 
