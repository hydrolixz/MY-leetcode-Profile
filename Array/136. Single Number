class Solution(object):
    def singleNumber(self, nums):
        d={}
        for char in nums:
            if char not in d.keys():
                d[char]=1
            else:
                d[char]+=1
        for key,value in d.items():
            if value==1:
                return key

        