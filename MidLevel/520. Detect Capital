class Solution(object):
    def detectCapitalUse(self, word):
       if all([char.isupper() for char in word]):
         return True
       elif all([char.islower() for char in word[1:]]) and word[0].isupper()==True:
         return True
       elif all([char.islower() for char in word]):
         return True
       return False

        