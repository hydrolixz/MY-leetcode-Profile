class Solution(object):
    def findMissingElements(self, nums):
        nums.sort()
        L=[]
        for i in range(len(nums)-1):
            if nums[i]+1!=nums[i+1]:
                L.extend(range((nums[i]+1),nums[i+1]))
        return L
