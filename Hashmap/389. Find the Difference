class Solution(object):
    def findTheDifference(self, s, t):
        s=sorted(s)
        t=sorted(t)
        y=""
        for i in range(len(s)):
            if s[i]!=t[i]:
                y=t[i]
                return y
        if y=="":
            return t[-1]
